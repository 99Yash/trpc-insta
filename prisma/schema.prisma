generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Like {
  id        String   @id @default(cuid())
  authorId  String   @unique
  postId    String
  createdAt DateTime @default(now())
  post      Post     @relation(fields: [postId], references: [id])

  @@index([postId, authorId], map: "Like_PostId_AuthorId_Index")
}

model Comment {
  id        String   @id @default(cuid())
  authorId  String   @unique
  postId    String
  text      String
  likes     Int
  createdAt DateTime @default(now())
  post      Post     @relation(fields: [postId], references: [id])

  @@index([postId, authorId], map: "Comment_PostId_AuthorId_Index")
}

model Post {
  id        String    @id @default(cuid())
  authorId  String    @unique
  caption   String
  picture   Json?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  likes     Like[]
  comments  Comment[]

  @@index([authorId], map: "Post_AuthorId_Index")
}

model Followers {
  id         String @id @default(cuid())
  followerId String @unique
}
